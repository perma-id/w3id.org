# .htaccess for platform material digital vocabs
RewriteEngine On

# Default response
# ---------------------------
# Rewrite rule to serve the TTL content from the vocabulary URI by default
RewriteRule ^(.*)$ https://raw.githubusercontent.com/ThomasBjarsch/ODE_AM/main/WAAMAO.ttl [R=303,L]
