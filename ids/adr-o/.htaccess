RewriteEngine on

# /ADL/ goes to the rendered records on Pages
RewriteRule ^ADL/(.*)$ https://gutza.github.io/ADR-O/ADL/$1 [R=302,L]

# Everything else goes to the repo
RewriteRule ^ https://github.com/Gutza/ADR-O [R=302,L]
