# Turn off MultiViews
Options -MultiViews

# Basic redirect for serving instances. Temporary until we get the subdomain.
RewriteEngine On
RewriteRule ^export/(.*) https://rdfexplorer.mint.isi.edu/wings/export/$1 [R=302,L]
