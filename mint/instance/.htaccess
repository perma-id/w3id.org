# Turn off MultiViews
Options -MultiViews

# Basic redirect for serving instances. Temporary until we get the subdomain.
RewriteEngine On
RewriteRule ^$1 http://ontosoft.isi.edu:7070/mint/instance/$1 [R=302,L]
