RewriteEngine On
RewriteBase /osdi/

# Redirige a la documentación HTML (para humanos)
RewriteCond %{HTTP_ACCEPT} text/html
RewriteRule ^$ https://osdi-ull.github.io/OSDi/index.html [R=302,L]

# Redirige a RDF/XML si lo piden
RewriteCond %{HTTP_ACCEPT} application/rdf\+xml
RewriteRule ^$ https://osdi-ull.github.io/OSDi/OSDi.owl [R=302,L]

# Redirige a Turtle si lo piden
RewriteCond %{HTTP_ACCEPT} text/turtle
RewriteRule ^$ https://osdi-ull.github.io/OSDi/OSDi.ttl [R=302,L]

# Fallback: siempre manda a la documentación
RewriteRule ^$ https://osdi-ull.github.io/OSDi/index.html [R=302,L]
