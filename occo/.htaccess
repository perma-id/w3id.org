# OCCO - Occupant-Centric Control Ontology
# Contact: Saif Alramahi <saif.alramahi@mail.bcu.ac.uk>
# Repository: https://github.com/YOUR-USERNAME/occo (update when you have one)

Options +FollowSymLinks
RewriteEngine on

# Redirect to ontology HTML documentation (default)
RewriteRule ^$ https://YOUR-HOSTING-URL/occo/ [R=303,L]

# Redirect to ontology file (Turtle)
RewriteCond %{HTTP_ACCEPT} text/turtle
RewriteRule ^$ https://YOUR-HOSTING-URL/occo/occo.ttl [R=303,L]

# Redirect to ontology file (RDF/XML)
RewriteCond %{HTTP_ACCEPT} application/rdf\+xml
RewriteRule ^$ https://YOUR-HOSTING-URL/occo/occo.owl [R=303,L]
