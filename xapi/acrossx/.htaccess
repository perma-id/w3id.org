
Options +FollowSymLinks
RewriteEngine on


# verb dataset
# ---------------------------
# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^verbs$ http://xapi.vocab.pub/datasets/acrossx/verbs [R=303]
RewriteRule ^verbs/$ http://xapi.vocab.pub/datasets/acrossx/verbs [R=303]

# Rewrite rule to serve Turtle content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} text/turtle
RewriteRule ^verbs$ http://xapi.vocab.pub/datasets/acrossx/verbs.ttl [R=303]
RewriteRule ^verbs/$ http://xapi.vocab.pub/datasets/acrossx/verbs.ttl [R=303]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^verbs$ http://xapi.vocab.pub/datasets/acrossx/verbs.jsonld [R=303]
RewriteRule ^verbs/$ http://xapi.vocab.pub/datasets/acrossx/verbs.jsonld [R=303]


# designed
# ---------------------------
# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^verbs/designed$ http://xapi.vocab.pub/datasets/acrossx/verbs#designed [R=303,NE]

# Rewrite rule to serve Turtle content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} text/turtle
RewriteRule ^verbs/designed$ http://xapi.vocab.pub/datasets/acrossx/verbs.ttl [R=303]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^verbs/designed$ http://xapi.vocab.pub/datasets/acrossx/verbs.jsonld [R=303]


# disliked
# ---------------------------
# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^verbs/disliked$ http://xapi.vocab.pub/datasets/acrossx/verbs#disliked [R=303,NE]

# Rewrite rule to serve Turtle content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} text/turtle
RewriteRule ^verbs/disliked$ http://xapi.vocab.pub/datasets/acrossx/verbs.ttl [R=303]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^verbs/disliked$ http://xapi.vocab.pub/datasets/acrossx/verbs.jsonld [R=303]


# edited
# ---------------------------
# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^verbs/edited$ http://xapi.vocab.pub/datasets/acrossx/verbs#edited [R=303,NE]

# Rewrite rule to serve Turtle content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} text/turtle
RewriteRule ^verbs/edited$ http://xapi.vocab.pub/datasets/acrossx/verbs.ttl [R=303]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^verbs/edited$ http://xapi.vocab.pub/datasets/acrossx/verbs.jsonld [R=303]


# evaluated
# ---------------------------
# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^verbs/evaluated$ http://xapi.vocab.pub/datasets/acrossx/verbs#evaluated [R=303,NE]

# Rewrite rule to serve Turtle content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} text/turtle
RewriteRule ^verbs/evaluated$ http://xapi.vocab.pub/datasets/acrossx/verbs.ttl [R=303]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^verbs/evaluated$ http://xapi.vocab.pub/datasets/acrossx/verbs.jsonld [R=303]


# liked
# ---------------------------
# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^verbs/liked$ http://xapi.vocab.pub/datasets/acrossx/verbs#liked [R=303,NE]

# Rewrite rule to serve Turtle content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} text/turtle
RewriteRule ^verbs/liked$ http://xapi.vocab.pub/datasets/acrossx/verbs.ttl [R=303]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^verbs/liked$ http://xapi.vocab.pub/datasets/acrossx/verbs.jsonld [R=303]

# posted
# ---------------------------
# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^verbs/posted$ http://xapi.vocab.pub/datasets/acrossx/verbs#posted [R=303,NE]

# Rewrite rule to serve Turtle content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} text/turtle
RewriteRule ^verbs/posted$ http://xapi.vocab.pub/datasets/acrossx/verbs.ttl [R=303]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^verbs/posted$ http://xapi.vocab.pub/datasets/acrossx/verbs.jsonld [R=303]

# reported
# ---------------------------
# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^verbs/reported$ http://xapi.vocab.pub/datasets/acrossx/verbs#reported [R=303,NE]

# Rewrite rule to serve Turtle content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} text/turtle
RewriteRule ^verbs/reported$ http://xapi.vocab.pub/datasets/acrossx/verbs.ttl [R=303]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^verbs/reported$ http://xapi.vocab.pub/datasets/acrossx/verbs.jsonld [R=303]


# was-assigned
# ---------------------------
# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^verbs/was-assigned$ http://xapi.vocab.pub/datasets/acrossx/verbs#was-assigned [R=303,NE]

# Rewrite rule to serve Turtle content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} text/turtle
RewriteRule ^verbs/was-assigned$ http://xapi.vocab.pub/datasets/acrossx/verbs.ttl [R=303]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^verbs/was-assigned$ http://xapi.vocab.pub/datasets/acrossx/verbs.jsonld [R=303]



# activities dataset
# ---------------------------
# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^activities$ http://xapi.vocab.pub/datasets/acrossx/activities [R=303]
RewriteRule ^activities/$ http://xapi.vocab.pub/datasets/acrossx/activities [R=303]

# Rewrite rule to serve Turtle content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} text/turtle
RewriteRule ^activities$ http://xapi.vocab.pub/datasets/acrossx/activities.ttl [R=303]
RewriteRule ^activities/$ http://xapi.vocab.pub/datasets/acrossx/activities.ttl [R=303]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^activities$ http://xapi.vocab.pub/datasets/acrossx/activities.jsonld [R=303]
RewriteRule ^activities/$ http://xapi.vocab.pub/datasets/acrossx/activities.jsonld [R=303]



# collaboration
# ---------------------------
# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^activities/collaboration$ http://xapi.vocab.pub/datasets/acrossx/activities#collaboration [R=303,NE]

# Rewrite rule to serve Turtle content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} text/turtle
RewriteRule ^activities/collaboration$ http://xapi.vocab.pub/datasets/acrossx/activities.ttl [R=303]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^activities/collaboration$ http://xapi.vocab.pub/datasets/acrossx/activities.jsonld [R=303]




# e-book
# ---------------------------
# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^activities/e-book$ http://xapi.vocab.pub/datasets/acrossx/activities#e-book [R=303,NE]

# Rewrite rule to serve Turtle content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} text/turtle
RewriteRule ^activities/e-book$ http://xapi.vocab.pub/datasets/acrossx/activities.ttl [R=303]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^activities/e-book$ http://xapi.vocab.pub/datasets/acrossx/activities.jsonld [R=303]



# face-to-face-discussion
# ---------------------------
# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^activities/face-to-face-discussion$ http://xapi.vocab.pub/datasets/acrossx/activities#face-to-face-discussion [R=303,NE]

# Rewrite rule to serve Turtle content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} text/turtle
RewriteRule ^activities/face-to-face-discussion$ http://xapi.vocab.pub/datasets/acrossx/activities.ttl [R=303]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^activities/face-to-face-discussion$ http://xapi.vocab.pub/datasets/acrossx/activities.jsonld [R=303]



# instant-response-system
# ---------------------------
# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^activities/instant-response-system$ http://xapi.vocab.pub/datasets/acrossx/activities#instant-response-system [R=303,NE]

# Rewrite rule to serve Turtle content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} text/turtle
RewriteRule ^activities/instant-response-system$ http://xapi.vocab.pub/datasets/acrossx/activities.ttl [R=303]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^activities/instant-response-system$ http://xapi.vocab.pub/datasets/acrossx/activities.jsonld [R=303]



# learning-plan
# ---------------------------
# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^activities/learning-plan$ http://xapi.vocab.pub/datasets/acrossx/activities#learning-plan [R=303,NE]

# Rewrite rule to serve Turtle content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} text/turtle
RewriteRule ^activities/learning-plan$ http://xapi.vocab.pub/datasets/acrossx/activities.ttl [R=303]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^activities/learning-plan$ http://xapi.vocab.pub/datasets/acrossx/activities.jsonld [R=303]



# message
# ---------------------------
# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^activities/message$ http://xapi.vocab.pub/datasets/acrossx/activities#message [R=303,NE]

# Rewrite rule to serve Turtle content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} text/turtle
RewriteRule ^activities/message$ http://xapi.vocab.pub/datasets/acrossx/activities.ttl [R=303]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^activities/message$ http://xapi.vocab.pub/datasets/acrossx/activities.jsonld [R=303]



# note
# ---------------------------
# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^activities/note$ http://xapi.vocab.pub/datasets/acrossx/activities#note [R=303,NE]

# Rewrite rule to serve Turtle content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} text/turtle
RewriteRule ^activities/note$ http://xapi.vocab.pub/datasets/acrossx/activities.ttl [R=303]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^activities/note$ http://xapi.vocab.pub/datasets/acrossx/activities.jsonld [R=303]



# online-discussion
# ---------------------------
# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^activities/online-discussion$ http://xapi.vocab.pub/datasets/acrossx/activities#online-discussion [R=303,NE]

# Rewrite rule to serve Turtle content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} text/turtle
RewriteRule ^activities/online-discussion$ http://xapi.vocab.pub/datasets/acrossx/activities.ttl [R=303]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^activities/online-discussion$ http://xapi.vocab.pub/datasets/acrossx/activities.jsonld [R=303]



# page
# ---------------------------
# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^activities/page$ http://xapi.vocab.pub/datasets/acrossx/activities#page [R=303,NE]

# Rewrite rule to serve Turtle content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} text/turtle
RewriteRule ^activities/page$ http://xapi.vocab.pub/datasets/acrossx/activities.ttl [R=303]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^activities/page$ http://xapi.vocab.pub/datasets/acrossx/activities.jsonld [R=303]



# printed-book
# ---------------------------
# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^activities/printed-book$ http://xapi.vocab.pub/datasets/acrossx/activities#printed-book [R=303,NE]

# Rewrite rule to serve Turtle content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} text/turtle
RewriteRule ^activities/printed-book$ http://xapi.vocab.pub/datasets/acrossx/activities.ttl [R=303]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^activities/printed-book$ http://xapi.vocab.pub/datasets/acrossx/activities.jsonld [R=303]



# printed-worksheet
# ---------------------------
# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^activities/printed-worksheet$ http://xapi.vocab.pub/datasets/acrossx/activities#printed-worksheet [R=303,NE]

# Rewrite rule to serve Turtle content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} text/turtle
RewriteRule ^activities/printed-worksheet$ http://xapi.vocab.pub/datasets/acrossx/activities.ttl [R=303]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^activities/printed-worksheet$ http://xapi.vocab.pub/datasets/acrossx/activities.jsonld [R=303]




# printed-assessment
# ---------------------------
# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^activities/printed-assessment$ http://xapi.vocab.pub/datasets/acrossx/activities#printed-assessment [R=303,NE]

# Rewrite rule to serve Turtle content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} text/turtle
RewriteRule ^activities/printed-assessment$ http://xapi.vocab.pub/datasets/acrossx/activities.ttl [R=303]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^activities/printed-assessment$ http://xapi.vocab.pub/datasets/acrossx/activities.jsonld [R=303]



# printed-worksheet
# ---------------------------
# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^activities/printed-worksheet$ http://xapi.vocab.pub/datasets/acrossx/activities#printed-worksheet [R=303,NE]

# Rewrite rule to serve Turtle content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} text/turtle
RewriteRule ^activities/printed-worksheet$ http://xapi.vocab.pub/datasets/acrossx/activities.ttl [R=303]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^activities/printed-worksheet$ http://xapi.vocab.pub/datasets/acrossx/activities.jsonld [R=303]



# video
# ---------------------------
# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^activities/video$ http://xapi.vocab.pub/datasets/acrossx/activities#video [R=303,NE]

# Rewrite rule to serve Turtle content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} text/turtle
RewriteRule ^activities/video$ http://xapi.vocab.pub/datasets/acrossx/activities.ttl [R=303]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^activities/video$ http://xapi.vocab.pub/datasets/acrossx/activities.jsonld [R=303]



# webpage
# ---------------------------
# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^activities/webpage$ http://xapi.vocab.pub/datasets/acrossx/activities#webpage [R=303,NE]

# Rewrite rule to serve Turtle content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} text/turtle
RewriteRule ^activities/webpage$ http://xapi.vocab.pub/datasets/acrossx/activities.ttl [R=303]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^activities/webpage$ http://xapi.vocab.pub/datasets/acrossx/activities.jsonld [R=303]


 
