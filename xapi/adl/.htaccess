
Options +FollowSymLinks
RewriteEngine on

# verb dataset
# ---------------------------

# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^verbs$ http://xapi.vocab.pub/datasets/adl/verbs [R=303]

# Rewrite rule to serve Turtle content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} text/turtle
RewriteRule ^verbs$ http://xapi.vocab.pub/datasets/adl/verb-metadata/verbs.ttl [R=303]


# abandoned
# ---------------------------

# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^verbs/abandoned$ http://xapi.vocab.pub/datasets/adl/verbs/#abandoned [R=303,NE]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^verbs/abandoned$ http://xapi.vocab.pub/datasets/adl/verb-metadata/abandoned.jsonld [R=303]

# Rewrite rule to serve JSON content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/json
RewriteRule ^verbs/abandoned$ http://xapi.vocab.pub/datasets/adl/verb-metadata/abandoned.json [R=303]


# answered
# ---------------------------

# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^verbs/answered$ http://xapi.vocab.pub/datasets/adl/verbs/#answered [R=303,NE]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^verbs/answered$ http://xapi.vocab.pub/datasets/adl/verb-metadata/answered.jsonld [R=303]

# Rewrite rule to serve JSON content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/json
RewriteRule ^verbs/answered$ http://xapi.vocab.pub/datasets/adl/verb-metadata/answered.json [R=303]


# asked
# ---------------------------

# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^verbs/asked$ http://xapi.vocab.pub/datasets/adl/verbs/#asked [R=303,NE]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^verbs/asked$ http://xapi.vocab.pub/datasets/adl/verb-metadata/asked.jsonld [R=303]

# Rewrite rule to serve JSON content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/json
RewriteRule ^verbs/asked$ http://xapi.vocab.pub/datasets/adl/verb-metadata/asked.json [R=303]



# attempted
# ---------------------------

# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^verbs/attempted$ http://xapi.vocab.pub/datasets/adl/verbs/#attempted [R=303,NE]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^verbs/attempted$ http://xapi.vocab.pub/datasets/adl/verb-metadata/attempted.jsonld [R=303]

# Rewrite rule to serve JSON content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/json
RewriteRule ^verbs/attempted$ http://xapi.vocab.pub/datasets/adl/verb-metadata/attempted.json [R=303]


# attended
# ---------------------------

# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^verbs/attended$ http://xapi.vocab.pub/datasets/adl/verbs/#attended [R=303,NE]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^verbs/attended$ http://xapi.vocab.pub/datasets/adl/verb-metadata/attended.jsonld [R=303]

# Rewrite rule to serve JSON content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/json
RewriteRule ^verbs/attended$ http://xapi.vocab.pub/datasets/adl/verb-metadata/attended.json [R=303]



# commented
# ---------------------------

# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^verbs/commented$ http://xapi.vocab.pub/datasets/adl/verbs/#commented [R=303,NE]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^verbs/commented$ http://xapi.vocab.pub/datasets/adl/verb-metadata/commented.jsonld [R=303]

# Rewrite rule to serve JSON content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/json
RewriteRule ^verbs/commented$ http://xapi.vocab.pub/datasets/adl/verb-metadata/commented.json [R=303]




# completed
# ---------------------------

# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^verbs/completed$ http://xapi.vocab.pub/datasets/adl/verbs/#completed [R=303,NE]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^verbs/completed$ http://xapi.vocab.pub/datasets/adl/verb-metadata/completed.jsonld [R=303]

# Rewrite rule to serve JSON content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/json
RewriteRule ^verbs/completed$ http://xapi.vocab.pub/datasets/adl/verb-metadata/completed.json [R=303]



# exited
# ---------------------------

# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^verbs/exited$ http://xapi.vocab.pub/datasets/adl/verbs/#exited [R=303,NE]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^verbs/exited$ http://xapi.vocab.pub/datasets/adl/verb-metadata/exited.jsonld [R=303]

# Rewrite rule to serve JSON content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/json
RewriteRule ^verbs/exited$ http://xapi.vocab.pub/datasets/adl/verb-metadata/exited.json [R=303]


# experienced
# ---------------------------

# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^verbs/experienced$ http://xapi.vocab.pub/datasets/adl/verbs/#experienced [R=303,NE]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^verbs/experienced$ http://xapi.vocab.pub/datasets/adl/verb-metadata/experienced.jsonld [R=303]

# Rewrite rule to serve JSON content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/json
RewriteRule ^verbs/experienced$ http://xapi.vocab.pub/datasets/adl/verb-metadata/experienced.json [R=303]



# failed
# ---------------------------

# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^verbs/failed$ http://xapi.vocab.pub/datasets/adl/verbs/#failed [R=303,NE]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^verbs/failed$ http://xapi.vocab.pub/datasets/adl/verb-metadata/failed.jsonld [R=303]

# Rewrite rule to serve JSON content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/json
RewriteRule ^verbs/failed$ http://xapi.vocab.pub/datasets/adl/verb-metadata/failed.json [R=303]




# imported
# ---------------------------

# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^verbs/imported$ http://xapi.vocab.pub/datasets/adl/verbs/#imported [R=303,NE]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^verbs/imported$ http://xapi.vocab.pub/datasets/adl/verb-metadata/imported.jsonld [R=303]


# Rewrite rule to serve JSON content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/json
RewriteRule ^verbs/imported$ http://xapi.vocab.pub/datasets/adl/verb-metadata/imported.json [R=303]



# initialized
# ---------------------------

# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^verbs/initialized$ http://xapi.vocab.pub/datasets/adl/verbs/#initialized [R=303,NE]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^verbs/initialized$ http://xapi.vocab.pub/datasets/adl/verb-metadata/initialized.jsonld [R=303]

# Rewrite rule to serve JSON content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/json
RewriteRule ^verbs/initialized$ http://xapi.vocab.pub/datasets/adl/verb-metadata/initialized.json [R=303]




# interacted
# ---------------------------

# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^verbs/interacted$ http://xapi.vocab.pub/datasets/adl/verbs/#interacted [R=303,NE]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^verbs/interacted$ http://xapi.vocab.pub/datasets/adl/verb-metadata/interacted.jsonld [R=303]

# Rewrite rule to serve JSON content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/json
RewriteRule ^verbs/interacted$ http://xapi.vocab.pub/datasets/adl/verb-metadata/interacted.json [R=303]






# launched
# ---------------------------

# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^verbs/launched$ http://xapi.vocab.pub/datasets/adl/verbs/#launched [R=303,NE]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^verbs/launched$ http://xapi.vocab.pub/datasets/adl/verb-metadata/launched.jsonld [R=303]

# Rewrite rule to serve JSON content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/json
RewriteRule ^verbs/launched$ http://xapi.vocab.pub/datasets/adl/verb-metadata/launched.json [R=303]





# logged-in
# ---------------------------

# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^verbs/logged-in$ http://xapi.vocab.pub/datasets/adl/verbs/#logged-in [R=303,NE]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^verbs/logged-in$ http://xapi.vocab.pub/datasets/adl/verb-metadata/logged-in.jsonld [R=303]

# Rewrite rule to serve JSON content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/json
RewriteRule ^verbs/logged-in$ http://xapi.vocab.pub/datasets/adl/verb-metadata/logged-in.json [R=303]






# logged-out
# ---------------------------

# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^verbs/logged-out$ http://xapi.vocab.pub/datasets/adl/verbs/#logged-out [R=303,NE]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^verbs/logged-out$ http://xapi.vocab.pub/datasets/adl/verb-metadata/logged-out.jsonld [R=303]

# Rewrite rule to serve JSON content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/json
RewriteRule ^verbs/logged-out$ http://xapi.vocab.pub/datasets/adl/verb-metadata/logged-out.json [R=303]






# mastered
# ---------------------------

# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^verbs/mastered$ http://xapi.vocab.pub/datasets/adl/verbs/#mastered [R=303,NE]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^verbs/mastered$ http://xapi.vocab.pub/datasets/adl/verb-metadata/mastered.jsonld [R=303]

# Rewrite rule to serve JSON content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/json
RewriteRule ^verbs/mastered$ http://xapi.vocab.pub/datasets/adl/verb-metadata/mastered.json [R=303]






# passed
# ---------------------------

# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^verbs/passed$ http://xapi.vocab.pub/datasets/adl/verbs/#passed [R=303,NE]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^verbs/passed$ http://xapi.vocab.pub/datasets/adl/verb-metadata/passed.jsonld [R=303]

# Rewrite rule to serve JSON content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/json
RewriteRule ^verbs/passed$ http://xapi.vocab.pub/datasets/adl/verb-metadata/passed.json [R=303]






# preferred
# ---------------------------

# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^verbs/preferred$ http://xapi.vocab.pub/datasets/adl/verbs/#preferred [R=303,NE]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^verbs/preferred$ http://xapi.vocab.pub/datasets/adl/verb-metadata/preferred.jsonld [R=303]

# Rewrite rule to serve JSON content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/json
RewriteRule ^verbs/preferred$ http://xapi.vocab.pub/datasets/adl/verb-metadata/preferred.json [R=303]




# progressed
# ---------------------------

# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^verbs/progressed$ http://xapi.vocab.pub/datasets/adl/verbs/#progressed [R=303,NE]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^verbs/progressed$ http://xapi.vocab.pub/datasets/adl/verb-metadata/progressed.jsonld [R=303]

# Rewrite rule to serve JSON content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/json
RewriteRule ^verbs/progressed$ http://xapi.vocab.pub/datasets/adl/verb-metadata/progressed.json [R=303]





# registered
# ---------------------------

# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^verbs/registered$ http://xapi.vocab.pub/datasets/adl/verbs/#registered [R=303,NE]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^verbs/registered$ http://xapi.vocab.pub/datasets/adl/verb-metadata/registered.jsonld [R=303]

# Rewrite rule to serve JSON content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/json
RewriteRule ^verbs/registered$ http://xapi.vocab.pub/datasets/adl/verb-metadata/registered.json [R=303]




# responded
# ---------------------------

# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^verbs/responded$ http://xapi.vocab.pub/datasets/adl/verbs/#responded [R=303,NE]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^verbs/responded$ http://xapi.vocab.pub/datasets/adl/verb-metadata/responded.jsonld [R=303]

# Rewrite rule to serve JSON content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/json
RewriteRule ^verbs/responded$ http://xapi.vocab.pub/datasets/adl/verb-metadata/responded.json [R=303]





# resumed
# ---------------------------

# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^verbs/resumed$ http://xapi.vocab.pub/datasets/adl/verbs/#resumed [R=303,NE]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^verbs/resumed$ http://xapi.vocab.pub/datasets/adl/verb-metadata/resumed.jsonld [R=303]

# Rewrite rule to serve JSON content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/json
RewriteRule ^verbs/resumed$ http://xapi.vocab.pub/datasets/adl/verb-metadata/resumed.json [R=303]







# satisfied
# ---------------------------

# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^verbs/satisfied$ http://xapi.vocab.pub/datasets/adl/verbs/#satisfied [R=303,NE]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^verbs/satisfied$ http://xapi.vocab.pub/datasets/adl/verb-metadata/satisfied.jsonld [R=303]

# Rewrite rule to serve JSON content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/json
RewriteRule ^verbs/satisfied$ http://xapi.vocab.pub/datasets/adl/verb-metadata/satisfied.json [R=303]






# scored
# ---------------------------

# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^verbs/scored$ http://xapi.vocab.pub/datasets/adl/verbs/#scored [R=303,NE]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^verbs/scored$ http://xapi.vocab.pub/datasets/adl/verb-metadata/scored.jsonld [R=303]

# Rewrite rule to serve JSON content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/json
RewriteRule ^verbs/scored$ http://xapi.vocab.pub/datasets/adl/verb-metadata/scored.json [R=303]






# shared
# ---------------------------

# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^verbs/shared$ http://xapi.vocab.pub/datasets/adl/verbs/#shared [R=303,NE]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^verbs/shared$ http://xapi.vocab.pub/datasets/adl/verb-metadata/shared.jsonld [R=303]

# Rewrite rule to serve JSON content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/json
RewriteRule ^verbs/shared$ http://xapi.vocab.pub/datasets/adl/verb-metadata/shared.json [R=303]






# suspended
# ---------------------------

# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^verbs/suspended$ http://xapi.vocab.pub/datasets/adl/verbs/#suspended [R=303,NE]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^verbs/suspended$ http://xapi.vocab.pub/datasets/adl/verb-metadata/suspended.jsonld [R=303]

# Rewrite rule to serve JSON content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/json
RewriteRule ^verbs/suspended$ http://xapi.vocab.pub/datasets/adl/verb-metadata/suspended.json [R=303]





# terminated
# ---------------------------

# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^verbs/terminated$ http://xapi.vocab.pub/datasets/adl/verbs/#terminated [R=303,NE]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^verbs/terminated$ http://xapi.vocab.pub/datasets/adl/verb-metadata/terminated.jsonld [R=303]

# Rewrite rule to serve JSON content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/json
RewriteRule ^verbs/terminated$ http://xapi.vocab.pub/datasets/adl/verb-metadata/terminated.json [R=303]






# waived
# ---------------------------

# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^verbs/waived$ http://xapi.vocab.pub/datasets/adl/verbs/#waived [R=303,NE]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^verbs/waived$ http://xapi.vocab.pub/datasets/adl/verb-metadata/waived.jsonld [R=303]

# Rewrite rule to serve JSON content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/json
RewriteRule ^verbs/terminated$ http://xapi.vocab.pub/datasets/adl/verb-metadata/waived.json [R=303]





# voided
# ---------------------------

# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^verbs/voided$ http://xapi.vocab.pub/datasets/adl/verbs/#voided [R=303,NE]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^verbs/voided$ http://xapi.vocab.pub/datasets/adl/verb-metadata/voided.jsonld [R=303]

# Rewrite rule to serve JSON content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/json
RewriteRule ^verbs/voided$ http://xapi.vocab.pub/datasets/adl/verb-metadata/voided.json [R=303]




