

Options +FollowSymLinks
RewriteEngine on

# abandoned
# ---------------------------

# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^abandoned$ http://xapi.vocab.pub/datasets/adl/verbs/#abandoned [R=303,NE]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^abandoned$ http://xapi.vocab.pub/datasets/adl/verbs/abandoned.jsonld [R=303]

# Rewrite rule to serve JSON content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/json
RewriteRule ^abandoned$ http://xapi.vocab.pub/datasets/adl/verbs/abandoned.json [R=303]


# answered
# ---------------------------

# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^answered$ http://xapi.vocab.pub/datasets/adl/verbs/#answered [R=303,NE]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^answered$ http://xapi.vocab.pub/datasets/adl/verbs/answered.jsonld [R=303]

# Rewrite rule to serve JSON content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/json
RewriteRule ^answered$ http://xapi.vocab.pub/datasets/adl/verbs/answered.json [R=303]


# asked
# ---------------------------

# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^asked$ http://xapi.vocab.pub/datasets/adl/verbs/#asked [R=303,NE]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^asked$ http://xapi.vocab.pub/datasets/adl/verbs/asked.jsonld [R=303]

# Rewrite rule to serve JSON content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/json
RewriteRule ^asked$ http://xapi.vocab.pub/datasets/adl/verbs/asked.json [R=303]



# attempted
# ---------------------------

# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^attempted$ http://xapi.vocab.pub/datasets/adl/verbs/#attempted [R=303,NE]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^attempted$ http://xapi.vocab.pub/datasets/adl/verbs/attempted.jsonld [R=303]

# Rewrite rule to serve JSON content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/json
RewriteRule ^attempted$ http://xapi.vocab.pub/datasets/adl/verbs/attempted.json [R=303]


# attended
# ---------------------------

# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^attended$ http://xapi.vocab.pub/datasets/adl/verbs/#attended [R=303,NE]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^attended$ http://xapi.vocab.pub/datasets/adl/verbs/attended.jsonld [R=303]

# Rewrite rule to serve JSON content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/json
RewriteRule ^attended$ http://xapi.vocab.pub/datasets/adl/verbs/attended.json [R=303]



# commented
# ---------------------------

# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^commented$ http://xapi.vocab.pub/datasets/adl/verbs/#commented [R=303,NE]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^commented$ http://xapi.vocab.pub/datasets/adl/verbs/commented.jsonld [R=303]

# Rewrite rule to serve JSON content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/json
RewriteRule ^commented$ http://xapi.vocab.pub/datasets/adl/verbs/commented.json [R=303]




# completed
# ---------------------------

# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^completed$ http://xapi.vocab.pub/datasets/adl/verbs/#completed [R=303,NE]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^completed$ http://xapi.vocab.pub/datasets/adl/verbs/completed.jsonld [R=303]

# Rewrite rule to serve JSON content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/json
RewriteRule ^completed$ http://xapi.vocab.pub/datasets/adl/verbs/completed.json [R=303]



# exited
# ---------------------------

# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^exited$ http://xapi.vocab.pub/datasets/adl/verbs/#exited [R=303,NE]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^exited$ http://xapi.vocab.pub/datasets/adl/verbs/exited.jsonld [R=303]

# Rewrite rule to serve JSON content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/json
RewriteRule ^exited$ http://xapi.vocab.pub/datasets/adl/verbs/exited.json [R=303]


# experienced
# ---------------------------

# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^experienced$ http://xapi.vocab.pub/datasets/adl/verbs/#experienced [R=303,NE]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^experienced$ http://xapi.vocab.pub/datasets/adl/verbs/experienced.jsonld [R=303]

# Rewrite rule to serve JSON content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/json
RewriteRule ^experienced$ http://xapi.vocab.pub/datasets/adl/verbs/experienced.json [R=303]



# failed
# ---------------------------

# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^failed$ http://xapi.vocab.pub/datasets/adl/verbs/#failed [R=303,NE]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^failed$ http://xapi.vocab.pub/datasets/adl/verbs/failed.jsonld [R=303]

# Rewrite rule to serve JSON content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/json
RewriteRule ^failed$ http://xapi.vocab.pub/datasets/adl/verbs/failed.json [R=303]




# imported
# ---------------------------

# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^imported$ http://xapi.vocab.pub/datasets/adl/verbs/#imported [R=303,NE]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^imported$ http://xapi.vocab.pub/datasets/adl/verbs/imported.jsonld [R=303]


# Rewrite rule to serve JSON content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/json
RewriteRule ^imported$ http://xapi.vocab.pub/datasets/adl/verbs/imported.json [R=303]



# initialized
# ---------------------------

# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^initialized$ http://xapi.vocab.pub/datasets/adl/verbs/#initialized [R=303,NE]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^initialized$ http://xapi.vocab.pub/datasets/adl/verbs/initialized.jsonld [R=303]

# Rewrite rule to serve JSON content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/json
RewriteRule ^initialized$ http://xapi.vocab.pub/datasets/adl/verbs/initialized.json [R=303]




# interacted
# ---------------------------

# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^interacted$ http://xapi.vocab.pub/datasets/adl/verbs/#interacted [R=303,NE]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^interacted$ http://xapi.vocab.pub/datasets/adl/verbs/interacted.jsonld [R=303]

# Rewrite rule to serve JSON content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/json
RewriteRule ^interacted$ http://xapi.vocab.pub/datasets/adl/verbs/interacted.json [R=303]






# launched
# ---------------------------

# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^launched$ http://xapi.vocab.pub/datasets/adl/verbs/#launched [R=303,NE]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^launched$ http://xapi.vocab.pub/datasets/adl/verbs/launched.jsonld [R=303]

# Rewrite rule to serve JSON content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/json
RewriteRule ^launched$ http://xapi.vocab.pub/datasets/adl/verbs/launched.json [R=303]





# logged-in
# ---------------------------

# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^logged-in$ http://xapi.vocab.pub/datasets/adl/verbs/#logged-in [R=303,NE]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^logged-in$ http://xapi.vocab.pub/datasets/adl/verbs/logged-in.jsonld [R=303]

# Rewrite rule to serve JSON content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/json
RewriteRule ^logged-in$ http://xapi.vocab.pub/datasets/adl/verbs/logged-in.json [R=303]






# logged-out
# ---------------------------

# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^logged-out$ http://xapi.vocab.pub/datasets/adl/verbs/#logged-out [R=303,NE]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^logged-out$ http://xapi.vocab.pub/datasets/adl/verbs/logged-out.jsonld [R=303]

# Rewrite rule to serve JSON content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/json
RewriteRule ^logged-out$ http://xapi.vocab.pub/datasets/adl/verbs/logged-out.json [R=303]






# mastered
# ---------------------------

# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^mastered$ http://xapi.vocab.pub/datasets/adl/verbs/#mastered [R=303,NE]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^mastered$ http://xapi.vocab.pub/datasets/adl/verbs/mastered.jsonld [R=303]

# Rewrite rule to serve JSON content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/json
RewriteRule ^mastered$ http://xapi.vocab.pub/datasets/adl/verbs/mastered.json [R=303]






# passed
# ---------------------------

# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^passed$ http://xapi.vocab.pub/datasets/adl/verbs/#passed [R=303,NE]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^passed$ http://xapi.vocab.pub/datasets/adl/verbs/passed.jsonld [R=303]

# Rewrite rule to serve JSON content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/json
RewriteRule ^passed$ http://xapi.vocab.pub/datasets/adl/verbs/passed.json [R=303]






# preferred
# ---------------------------

# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^preferred$ http://xapi.vocab.pub/datasets/adl/verbs/#preferred [R=303,NE]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^preferred$ http://xapi.vocab.pub/datasets/adl/verbs/preferred.jsonld [R=303]

# Rewrite rule to serve JSON content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/json
RewriteRule ^preferred$ http://xapi.vocab.pub/datasets/adl/verbs/preferred.json [R=303]




# progressed
# ---------------------------

# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^progressed$ http://xapi.vocab.pub/datasets/adl/verbs/#progressed [R=303,NE]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^progressed$ http://xapi.vocab.pub/datasets/adl/verbs/progressed.jsonld [R=303]

# Rewrite rule to serve JSON content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/json
RewriteRule ^progressed$ http://xapi.vocab.pub/datasets/adl/verbs/progressed.json [R=303]





# registered
# ---------------------------

# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^registered$ http://xapi.vocab.pub/datasets/adl/verbs/#registered [R=303,NE]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^registered$ http://xapi.vocab.pub/datasets/adl/verbs/registered.jsonld [R=303]

# Rewrite rule to serve JSON content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/json
RewriteRule ^registered$ http://xapi.vocab.pub/datasets/adl/verbs/registered.json [R=303]




# responded
# ---------------------------

# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^responded$ http://xapi.vocab.pub/datasets/adl/verbs/#responded [R=303,NE]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^responded$ http://xapi.vocab.pub/datasets/adl/verbs/responded.jsonld [R=303]

# Rewrite rule to serve JSON content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/json
RewriteRule ^responded$ http://xapi.vocab.pub/datasets/adl/verbs/responded.json [R=303]





# resumed
# ---------------------------

# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^resumed$ http://xapi.vocab.pub/datasets/adl/verbs/#resumed [R=303,NE]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^resumed$ http://xapi.vocab.pub/datasets/adl/verbs/resumed.jsonld [R=303]

# Rewrite rule to serve JSON content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/json
RewriteRule ^resumed$ http://xapi.vocab.pub/datasets/adl/verbs/resumed.json [R=303]







# satisfied
# ---------------------------

# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^satisfied$ http://xapi.vocab.pub/datasets/adl/verbs/#satisfied [R=303,NE]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^satisfied$ http://xapi.vocab.pub/datasets/adl/verbs/satisfied.jsonld [R=303]

# Rewrite rule to serve JSON content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/json
RewriteRule ^satisfied$ http://xapi.vocab.pub/datasets/adl/verbs/satisfied.json [R=303]






# scored
# ---------------------------

# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^scored$ http://xapi.vocab.pub/datasets/adl/verbs/#scored [R=303,NE]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^scored$ http://xapi.vocab.pub/datasets/adl/verbs/scored.jsonld [R=303]

# Rewrite rule to serve JSON content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/json
RewriteRule ^scored$ http://xapi.vocab.pub/datasets/adl/verbs/scored.json [R=303]






# shared
# ---------------------------

# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^shared$ http://xapi.vocab.pub/datasets/adl/verbs/#shared [R=303,NE]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^shared$ http://xapi.vocab.pub/datasets/adl/verbs/shared.jsonld [R=303]

# Rewrite rule to serve JSON content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/json
RewriteRule ^shared$ http://xapi.vocab.pub/datasets/adl/verbs/shared.json [R=303]






# suspended
# ---------------------------

# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^suspended$ http://xapi.vocab.pub/datasets/adl/verbs/#suspended [R=303,NE]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^suspended$ http://xapi.vocab.pub/datasets/adl/verbs/suspended.jsonld [R=303]

# Rewrite rule to serve JSON content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/json
RewriteRule ^suspended$ http://xapi.vocab.pub/datasets/adl/verbs/suspended.json [R=303]





# terminated
# ---------------------------

# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^terminated$ http://xapi.vocab.pub/datasets/adl/verbs/#terminated [R=303,NE]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^terminated$ http://xapi.vocab.pub/datasets/adl/verbs/terminated.jsonld [R=303]

# Rewrite rule to serve JSON content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/json
RewriteRule ^terminated$ http://xapi.vocab.pub/datasets/adl/verbs/terminated.json [R=303]






# waived
# ---------------------------

# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^waived$ http://xapi.vocab.pub/datasets/adl/verbs/#waived [R=303,NE]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^waived$ http://xapi.vocab.pub/datasets/adl/verbs/waived.jsonld [R=303]

# Rewrite rule to serve JSON content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/json
RewriteRule ^terminated$ http://xapi.vocab.pub/datasets/adl/verbs/waived.json [R=303]





# voided
# ---------------------------

# Rewrite rule to serve HTML content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} !application/rdf\+xml.*(text/html|application/xhtml\+xml)
RewriteCond %{HTTP_ACCEPT} text/html [OR]
RewriteCond %{HTTP_ACCEPT} application/xhtml\+xml [OR]
RewriteCond %{HTTP_USER_AGENT} ^Mozilla/.*
RewriteRule ^voided$ http://xapi.vocab.pub/datasets/adl/verbs/#voided [R=303,NE]

# Rewrite rule to serve JSON-LD content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/ld\+json
RewriteRule ^voided$ http://xapi.vocab.pub/datasets/adl/verbs/voided.jsonld [R=303]

# Rewrite rule to serve JSON content from the vocabulary URI if requested
RewriteCond %{HTTP_ACCEPT} application/json
RewriteRule ^voided$ http://xapi.vocab.pub/datasets/adl/verbs/voided.json [R=303]



