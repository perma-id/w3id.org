Options +FollowSymLinks
RewriteEngine on

# Permanent identifier for PyEuropePMC RDF data
# Redirects all requests under /pyeuropepmc/ to the corresponding RDF files
# hosted on GitHub raw.githubusercontent.com
#
# This enables resolvable URIs for linked data generated by the PyEuropePMC library.
# URIs like https://w3id.org/pyeuropepmc/paper/doi will redirect to
# https://raw.githubusercontent.com/JonasHeinickeBio/pyeuropepmc/main/data/paper/doi
#
# Using 302 (temporary) redirect initially; change to 301 (permanent) once tested.

RewriteRule ^(.*)$ https://raw.githubusercontent.com/JonasHeinickeBio/pyeuropepmc/main/data/$1 [R=302,L]