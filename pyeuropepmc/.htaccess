RewriteEngine on

# Redirect all requests under /pyeuropepmc/ to the corresponding path in the GitHub raw data
# This makes RDF URIs resolvable for PyEuropePMC data

RewriteRule ^(.*)$ https://raw.githubusercontent.com/JonasHeinickeBio/pyeuropepmc/main/data/$1 [R=302,L]