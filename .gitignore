*.Rproj
*.Rproj.user
*.py[co]
*.sw[nop]
*~
.DS_Store
.vscode
.idea/*

# Dependencies of tools/ (see tools/check)
node_modules/

# Build output of the documentation site.
/docs/.vitepress/cache/
/docs/.vitepress/dist/

# Self-signed certificates generated for local HTTPS testing, so that a private
# key cannot be committed by accident. Anchored, so it never shadows an
# identifier directory of the same name. See docs/guides/local-server.md.
/tools/server/tls/
